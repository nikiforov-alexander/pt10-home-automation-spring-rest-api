spring.data.rest.basePath = /api/v1

# database settings: I have no idea how to change them otherwise yet
# I added db_close_on_exit=false, because that was suggested by tests log
spring.datasource.url = jdbc:h2:mem:testing;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.password =
spring.datasource.username =
spring.datasource.driver-class-name= org.h2.Driver
spring.datasource.schema = create-drop

# server port for testing
server.port = 8081